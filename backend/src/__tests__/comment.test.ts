import request from 'supertest';
import app from '../app';

describe('Comment APIs', () => {
  it('should fetch comments by post', async () => {
    // const postId = '1';

    // const res = await request(app).get(`/api/v1/comments/${postId}`);

    // expect(res.statusCode).toBe(200);
  });
});
